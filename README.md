# SC_ML_4
This project builds a hand gesture recognition system using the Leap Gesture Recognition dataset. Hand images are loaded, resized, and divided into training, validation, and test sets. A CNN model learns to classify 10 gesture types. The model is evaluated with test accuracy and sample prediction images to demonstrate a good gesture classification.
